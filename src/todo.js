class Todo {
    constructor(title, description, dueDate, priority) {
      this.title = title;
      this.description = description;
      this.dueDate = dueDate;
      this.priority = priority;
      // You can add more properties like notes or a checklist here.
    }
  }
  
  export { Todo };